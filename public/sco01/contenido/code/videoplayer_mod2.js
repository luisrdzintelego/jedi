 
// Create a player.
var player = videojs('my-video1');
var track_current = null;
var audioTrackList = null;

player.on('ended', function() {
  console.log("🚀 ~ ended:")
  if (chk[1] == 0) {
    chk[1] = 1;
    set_vars();
  }
  chk_btns(1)
});


/* Cambia el tamaño al div que lo contiene */
player.on('canplay', function(){
  //console.log("load.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

player.on('loadstart', function(){
  //console.log("loadstart.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

// Create a player.
var player = videojs('my-video2');
var track_current = null;
var audioTrackList = null;

player.on('ended', function () {
  console.log("🚀 ~ ended:")
  if (chk[12] == 0) {
    chk[12] = 1;
    set_vars();
  }
  chk_btns(12)
});


/* Cambia el tamaño al div que lo contiene */
player.on('canplay', function () {
  //console.log("load.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

player.on('loadstart', function () {
  //console.log("loadstart.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

// Create a player.
var player = videojs('my-video3');
var track_current = null;
var audioTrackList = null;

player.on('ended', function () {
  console.log("🚀 ~ ended:")
  if (chk[15] == 0) {
    chk[15] = 1;
    set_vars();
  }
  chk_btns(15)
});


/* Cambia el tamaño al div que lo contiene */
player.on('canplay', function () {
  //console.log("load.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

player.on('loadstart', function () {
  //console.log("loadstart.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

// Create a player.
var player = videojs('my-video4');
var track_current = null;
var audioTrackList = null;

player.on('ended', function () {
  console.log("🚀 ~ ended:")
  if (chk[21] == 0) {
    chk[21] = 1;
    set_vars();
  }
  chk_btns(21)
});


/* Cambia el tamaño al div que lo contiene */
player.on('canplay', function () {
  //console.log("load.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

player.on('loadstart', function () {
  //console.log("loadstart.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

// Create a player.
var player = videojs('my-video5');
var track_current = null;
var audioTrackList = null;

player.on('ended', function () {
  console.log("🚀 ~ ended:")
  if (chk[22] == 0) {
    chk[22] = 1;
    set_vars();
  }
  chk_btns(22)
});


/* Cambia el tamaño al div que lo contiene */
player.on('canplay', function () {
  //console.log("load.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

player.on('loadstart', function () {
  //console.log("loadstart.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

// Create a player.
var player = videojs('my-video6');
var track_current = null;
var audioTrackList = null;

player.on('ended', function () {
  console.log("🚀 ~ ended:")
  if (chk[25] == 0) {
    chk[25] = 1;
    set_vars();
  }
  chk_btns(25)
});


/* Cambia el tamaño al div que lo contiene */
player.on('canplay', function () {
  //console.log("load.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})

player.on('loadstart', function () {
  //console.log("loadstart.....")
  let demoId = document.querySelector('.my-video-dimensions.vjs-fluid:not(.vjs-audio-only-mode)');
  //demoId.setAttribute('style', 'padding-top: 100%');
})